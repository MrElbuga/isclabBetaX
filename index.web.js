import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('isclabbeta', () => App);
AppRegistry.runApplication('isclabbeta', {
    rootTag: document.getElementById('root'),
});